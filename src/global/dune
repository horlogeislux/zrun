(rule
 (copy %{project_root}/config.ml config.ml))

(library
 (name global_lib)
 (public_name zrun.global_lib)
 (libraries zelus.compat zrun.parse_lib)
 (flags
  (:standard -open Zelus_compat -open Parse_lib)))
